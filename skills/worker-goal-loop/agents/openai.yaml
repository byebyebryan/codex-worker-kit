interface:
  display_name: "Worker Goal Loop"
  short_description: "Run a goal with Luna and adaptive primary support"
  default_prompt: "Use $worker-goal-loop to complete this goal with Luna by default, primary handling of difficult portions, and optional Sol workers when separate context helps."

policy:
  allow_implicit_invocation: false
