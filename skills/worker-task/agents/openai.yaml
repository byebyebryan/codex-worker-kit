interface:
  display_name: "Worker Task"
  short_description: "Cost-aware routing for one bounded task"
  default_prompt: "Use $worker-task to select the cost-effective worker tier, delegate, and review this bounded implementation task without creating a goal."

policy:
  allow_implicit_invocation: false
