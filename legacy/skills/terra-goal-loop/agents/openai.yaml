interface:
  display_name: "Legacy Terra Goal Loop"
  short_description: "Run a judgment-heavy Terra implementation loop"
  default_prompt: "Use $terra-goal-loop to assess readiness and implement this judgment-heavy checkpoint with the main agent as architect and terra-xhigh as worker."

policy:
  allow_implicit_invocation: false
